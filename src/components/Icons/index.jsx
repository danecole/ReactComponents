import React from 'react';
import SvgIcon from './SvgIcon';

const icon = (type, { size, title, set }) => (
  <SvgIcon type={type} size={size} title={title} set={set} />
);

const icons = {
  
};

export default icons;
